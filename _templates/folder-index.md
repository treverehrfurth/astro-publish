---
title:
---

<!-- Save as `index.md` inside any folder to make that folder a clickable
     destination in the left nav. The H1 falls back to the folder name when
     `title` is empty. -->

## Notes in this folder

```dataview
TABLE date AS "Date"
FROM "<folder-path>"
SORT date DESC
```
